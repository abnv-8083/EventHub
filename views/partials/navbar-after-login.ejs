<nav class="navbar navbar-expand-lg navbar-dark w-100">
    <div class="container">
        <a class="navbar-brand logo" href="/">EVENT<span>HUB</span></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mx-auto mb-2 mb-lg-0 d-flex align-items-center justify-content-center gap-lg-4 gap-3">
                <li class="nav-item">
                    <a class="nav-link" style="color: #ffffff; font-size: 1.15rem; font-weight: 500;" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" style="color: #ffffff; font-size: 1.15rem; font-weight: 500;" href="/events">Events</a>
                </li>
                <li class="nav-item">
                    <% if (locals.user && locals.user.isOrganizer) { %>
                        <a class="nav-link" style="color: #3498DB; font-size: 1.15rem; font-weight: 600;" href="/organizer/dashboard">
                            <i class="fa fa-building" style="margin-right: 0.3rem;"></i>Organizer Portal
                        </a>
                    <% } else if (locals.user && locals.user.hasPendingOrganizer) { %>
                        <a class="nav-link" style="color: #E67E22; font-size: 1.1rem; font-weight: 500;" href="/user/organizer/register">
                            <i class="fa fa-hourglass-half" style="margin-right: 0.3rem;"></i>Pending Approval
                        </a>
                    <% } else { %>
                        <a class="nav-link" style="color: #ffffff; font-size: 1.15rem; font-weight: 500;" href="/become-organizer">Organizer</a>
                    <% } %>
                </li>
                <li class="nav-item">
                    <a class="nav-link" style="color: #ffffff; font-size: 1.15rem; font-weight: 500;" href="/about">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" style="color: #ffffff; font-size: 1.15rem; font-weight: 500;" href="/contact">Contact</a>
                </li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center gap-2" href="#" id="navbarDropdown"
                        role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <div style="text-align: right; line-height: 1.1;">
                            <div style="font-weight: 600; font-size: 1 rem; color: #fff;">
                                <%= user.name %>
                            </div>
                            <!-- <div style="font-size: 0.7rem; color: var(--primary);">Member</div> -->
                        </div>
                        <div
                            style="width: 40px; height: 40px; background: var(--bg-surface); border-radius: 50%; display: flex; align-items: center; justify-content: center; overflow: hidden; border: 1px solid var(--border);">
                            <% if (user && user.avatar_url && user.avatar_url !== 'None') { %>
                                <img src="<%= user.avatar_url %>" alt="Profile" style="width: 100%; height: 100%; object-fit: cover;" referrerpolicy="no-referrer">
                            <% } else { %>
                                <i class="fa fa-user" style="font-size: 0.8rem; color: var(--primary);"></i>
                            <% } %>
                        </div>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end glass-dark shadow border-0 p-2" aria-labelledby="navbarDropdown" style="margin-top: 0.5rem; min-width: 220px; border-radius: 12px; border: 1px solid var(--border) !important;">
                        <li>
                            <a class="dropdown-item d-flex align-items-center py-2 px-3" href="/profile" style="border-radius: 8px;">
                                <% if (user && user.avatar_url && user.avatar_url !== 'None') { %>
                                    <div style="width: 24px; height: 24px; border-radius: 50%; overflow: hidden; margin-right: 1rem; border: 1px solid var(--border);">
                                        <img src="<%= user.avatar_url %>" alt="Profile" style="width: 100%; height: 100%; object-fit: cover;" referrerpolicy="no-referrer">
                                    </div>
                                <% } else { %>
                                    <i class="fa fa-user-circle me-3" style="font-size: 1.1rem; color: var(--text-muted);"></i>
                                <% } %>
                                <span style="font-weight: 500; font-size: 0.95rem;">My Profile</span>
                            </a>
                        </li>
                        <li>
                            <hr class="dropdown-divider" style="opacity: 0.1; margin: 0.5rem 0;">
                        </li>
                        <li>
                            <form class="ajax-form m-0 p-0" action="/user/logout" method="POST">
                                <button type="submit" class="dropdown-item text-danger d-flex align-items-center py-2 px-3" style="background: none; border: none; width: 100%; text-align: left; border-radius: 8px; transition: all 0.2s;">
                                    <i class="fa fa-sign-out-alt me-3" style="font-size: 1.1rem;"></i>
                                    <span style="font-weight: 500; font-size: 0.95rem;">Logout</span>
                                </button>
                            </form>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>